/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.pixabay.com','p16-sign-va.tiktokcdn.com'], // Add other domains if needed
      },
}

module.exports = nextConfig
